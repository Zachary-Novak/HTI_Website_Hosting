name: Push to S3

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Node
        uses: actions/setup-node@v2
      - name: Setup AWS CLI
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: AKIA2WPRRFJHTXOV6I7X
          aws-secret-access-key: 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
          aws-region: us-east-2
      - name: Sync files to S3 bucket
        run: aws s3 sync * s3://testings3pushbucket --delete
